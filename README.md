AutoWash Admin Panel

How to run (dev):
1. cd admin-panel
2. npm install
3. npm run dev
Open: http://localhost:5174

Note: API base URL is taken from VITE_API_URL env var. By default frontend expects backend at http://localhost:8000


